{
    "name": "backend",
    "version": "1.0.0",
    "type": "module",
    "main": "./lib/app.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "tsc",
        "dev": "nodemon",
        "start": "npm run build && node lib/app.js"
    },
    "devDependencies": {
        "nodemon": "^3.0.1",
        "typescript": "^5.2.2"
    },
    "dependencies": {
        "@apollo/server": "^4.9.4",
        "dotenv": "^16.3.1",
        "graphql": "^16.8.1"
    }
}
